# 课程报告待补充材料清单

> 报告主体已完成，`main.tex` 可用 XeLaTeX 编译，输出 `main.pdf`（10 页）。
> 已包含 1 张 TikZ 架构图、4 张实验截图、6 段源代码、2 张表格。
>
> 下表是建议补充的截图和实验，按优先级排列。报告正文已为每一项留好文字描述，
> 不补也能交付，补上后"主要操作过程"会更完整、更有说服力。
>
> 补完截图后把图放进 `figures/` 目录，英文命名，告诉我图名和想插在哪一节，
> 我来加 `\includegraphics` 和图注。

## 必补（作业核心要求，目前只有文字）

| # | 内容 | 为什么需要 | 怎么获取 |
|---|------|-----------|---------|
| 1 | 后端启动成功日志（对应第 2.2 节） | 现有的"控制台截图"其实是 H2 网页登录页，不是后端启动日志 | 在 `demo/` 目录跑 `./gradlew bootRun`，截显示 `Started DemoApplication` 和 `Tomcat started on port 8080` 的终端 |
| 2 | 后台 Java 异常堆栈输出（对应第 4.1 节） | 报告说 GlobalExceptionHandler 会 `printStackTrace()`，但没有实际堆栈图 | 后端开着时，浏览器访问 `http://localhost:8080/api/test/null-pointer?trigger=true` 和 `.../arithmetic?divisor=0`，截后端控制台打印的 `NullPointerException` 和 `ArithmeticException` 完整堆栈 |

## 建议补（让"操作过程"和"用户行为"更完整）

| # | 内容 | 对应章节 | 怎么获取 |
|---|------|---------|---------|
| 3 | Bugly 注册 / 创建应用 / 拿到 AppId | 3.1、3.2 | 在 `bugly.qq.com` 注册、新建产品，平台选 Android，截创建页和创建成功后显示的 AppId，代码里用的是 `3f957d9605` |
| 4 | App 在模拟器运行和崩溃测试 | 4.2 | Android Studio Run 到模拟器，截 App 登录、列表、详情页，点菜单的崩溃测试弹框，触发崩溃后截 logcat 里 Bugly 的上报日志 |
| 5 | 用户行为和页面访问顺序结果 | 4.3 | 操作几个页面后，浏览器访问 `http://localhost:8080/api/behavior/session/{你的sessionId}`，截返回的 JSON，重点看 `pageSequence` 字段，也可以在 H2 控制台查 `user_behavior` 表 |

## 选补（锦上添花）

| # | 内容 | 怎么获取 |
|---|------|---------|
| 6 | 重新构建 APK 成功输出 | `cd android-app && ./gradlew assembleDebug`，截 `BUILD SUCCESSFUL` |
| 7 | Bugly 性能、CPU、内存、网络数据 | Bugly 控制台对应页截图，前提是 SDK 采集到 |

## 编译方法

```bash
cd /Users/xuyuhong/Documents/SCUT/课程报告
xelatex main.tex      # 编译两次，确保交叉引用正确
xelatex main.tex
# 产物：main.pdf
```
